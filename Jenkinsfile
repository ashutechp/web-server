pipeline {
    
agent any

stages {
    
        stage("Parallel Tasks"){
        parallel { 
                steps{
                echo "First Job"
            }
        }
        }
                    
  }
}
