pipeline {
agent any
stages {

      stage("Parallel Tasks"){
            parallel { 
                stage("parallel 1") {
                steps{
                echo "First Job"
            }
          }
        }
        }
                    
  }
}
